export interface Collection {
    id: string;
    name: string;
    description: string;
    location: string;
    date: string;
    occurrencesCount: number;
    institution: string;
    ownerId: string;
}
